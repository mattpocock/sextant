import React from "react";
import { Layout } from "./Layout";

export default {
  title: "Components/Layout",
  parameters: {
    layout: "fullscreen",
  },
};

export const Default = () => {
  return <Layout />;
};
