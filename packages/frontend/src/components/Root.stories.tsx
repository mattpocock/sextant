import React from "react";
import { Heading } from "./Heading";
import { Layout } from "./Layout";

export default {
  title: "Components",
};

export const CreateFlow = () => {
  return (
    <Layout>
      <Heading>New Flow</Heading>
    </Layout>
  );
};
